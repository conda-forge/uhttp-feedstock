{% set name = "uhttp" %}
{% set version = "1.3.5" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/uhttp-{{ version }}.tar.gz
  sha256: d47cc426245d8e72f3f20ca142f977060fe158d7c092eac2a82941fcb9eaae6a

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.9,<4.0
    - poetry-core
    - pip
  run:
    - python >=3.9,<4.0

test:
  imports:
    - uhttp
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/0x67757300/uHTTP
  doc_url: https://0x67757300.github.io/uHTTP/uhttp.html
  summary: ASGI micro framework
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - thewchan
